# Capabilities

Each capability describes a stable behavior that is currently implemented and
covered by source or tests in this repository. Historical plans and
project-specific migration records are intentionally outside this directory.

Start with the [capability index](capability-index.md).
